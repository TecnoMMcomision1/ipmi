* Poner como comentario // el apellido y nombre de quien entrega

* Tanto el nombre del repositorio, como el de las carpetas y de los archivos dentro de ellas
se escriben en minúsculas.

* No es necesario agregar el .pde al archivo de Processing. El programa lo hace automáticamente.

* Las imágenes y archivos adicionales van en la carpeta data, no sueltas en el sketch.

* NUNCA usar loadImage() o loadFont() en el void draw()

*NO usar el modelo de color hexadecimal (el que lleva #)
